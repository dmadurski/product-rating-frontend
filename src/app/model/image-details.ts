export class ImageDetails {
    imageId: string;
    fileName: string;

    constructor(imageId: string, fileName: string) {
        this.imageId = imageId;
        this.fileName = fileName;

    }
}
